on: [push]

jobs:
  build:
    runs-on: node-slim
    steps:
      - run: apk add --no-cache git

      - uses: actions/checkout@v3
        with:
          submodules: true

      - run: npm ci
      - run: npx quartz build

      - run: rm -rf /srv/notes/
      - run: cp -r public/ /srv/notes/
